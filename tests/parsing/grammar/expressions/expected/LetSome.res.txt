let foo x y =
  ((match x with
    | Some vx ->
        let x1 = vx + 1 in
        (match y with
         | Some { vy; vz = 3 } -> let y1 = vy + 1 in Some (x1 + y1)
         | _ -> None)
    | _ -> None)
  [@ns.braces ])